import "./ImportHeader.css";

export default function ImportHeader() {
  return (
    <div className="importHeader">
      <h2>Import Two Files (Paginated)</h2>
      <div className="importHeaderSub">
        Upload CSV/Excel, load an existing import, fix missing headers, sort rows,
        replace files, or delete an import.
      </div>
    </div>
  );
}